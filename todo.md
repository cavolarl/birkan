# TODO list for birkan

## System tasks

- Make sure notes are saved to sqlite for persistance
- Make sure seatbelt is used to keep the agents inside the workspace

## UI tasks

### Setup panel improvements

- [x] Welcome / intro screen — explain what the setup does before anything happens
- [x] Step progress indicator — show where the user is (e.g. Step 1 of 3)
- [ ] Per-item accept / reject controls — individual toggle per project suggestion (currently only "Create all")
- [ ] Help text / field descriptions — guidance for the directory input and other fields
- [ ] Confirmation / success state — clear "you're done!" screen after accepting projects
- [x] Skip / manual setup option — escape hatch to bypass AI setup
- [ ] Back / undo navigation — go back a step without resetting everything
- [ ] Error state — clear message if the scan or Claude call fails

## QoL tasks

- Placeholder

### General improvents

- Write readme.md to contain all instructions
